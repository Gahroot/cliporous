// Renderer test setup — currently empty. jsdom and globals are configured
// declaratively in vitest.config.ts. This file exists to satisfy the
// `setupFiles` reference and serves as a hook for future global mocks
// (e.g. jest-dom matchers, ResizeObserver polyfills).
