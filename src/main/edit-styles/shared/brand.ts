/**
 * Brand color tokens — single source of truth.
 *
 * Mirrors the renderer-side CSS custom properties in
 * src/renderer/src/assets/index.css so the main process and Remotion
 * compositions render the same identity.
 *
 *   BRAND_BG     #23100c  deep warm brown (page / no-broll backdrop)
 *   BRAND_FG     #f6ecd9  warm cream (primary text on BRAND_BG)
 *   BRAND_ACCENT #9f75ff  violet (caption emphasis, quote marks, headline accents)
 */

export const BRAND_BG = '#23100c'
export const BRAND_FG = '#f6ecd9'
export const BRAND_ACCENT = '#9f75ff'
