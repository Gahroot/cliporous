import type { LongformArchetypeTemplate } from './types'

/**
 * section-header — purple pill section divider (Remotion-rendered) at topic
 * transitions. A short, punchy beat between major ideas.
 */
export const sectionHeader: LongformArchetypeTemplate = {
  archetype: 'section-header',
  defaultDurationSec: 3
}
