import type { LongformArchetypeTemplate } from './types'

/**
 * concept-card — full-frame graphic card (Remotion-rendered) shown over the
 * continuing narration. Holds long enough to read without losing pace.
 */
export const conceptCard: LongformArchetypeTemplate = {
  archetype: 'concept-card',
  defaultDurationSec: 4
}
