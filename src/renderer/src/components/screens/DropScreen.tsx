/**
 * DropScreen — initial entry. Drop zone + URL/path input + recent projects.
 *
 * Stub: full implementation lives in the dedicated DropScreen task. This
 * placeholder renders an empty centered frame so the shell router compiles
 * and routes correctly when `pipeline.stage === 'idle'`.
 */
export function DropScreen(): React.JSX.Element {
  return (
    <div className="flex h-full w-full items-center justify-center">
      <div className="text-muted-foreground text-sm">DropScreen</div>
    </div>
  )
}
