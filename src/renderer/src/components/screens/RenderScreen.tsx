/**
 * RenderScreen — per-clip render progress list and post-render success
 * summary.
 *
 * Stub: full implementation lives in the dedicated RenderScreen task.
 */
export function RenderScreen(): React.JSX.Element {
  return (
    <div className="flex h-full w-full items-center justify-center">
      <div className="text-muted-foreground text-sm">RenderScreen</div>
    </div>
  )
}
